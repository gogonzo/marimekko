% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-marimekko-text.R
\name{geom_marimekko_label}
\alias{geom_marimekko_label}
\title{Add labels with background to a marimekko plot}
\usage{
geom_marimekko_label(
  mapping = NULL,
  data = NULL,
  position = "identity",
  ...,
  gap = 0.01,
  gap_x = NULL,
  gap_y = NULL,
  standardize = FALSE,
  residuals = FALSE,
  size = 3.5,
  colour = "black",
  label.padding = unit(0.15, "lines"),
  na.rm = FALSE,
  show.legend = FALSE,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. Requires \code{x}, \code{fill}, and
\code{label}. Use \code{\link[ggplot2:aes_eval]{ggplot2::after_stat()}} for computed variables.}

\item{data}{A data frame.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Additional arguments passed to the layer.}

\item{gap}{Numeric. Gap between tiles. Default \code{0.01}.}

\item{gap_x}{Numeric. Horizontal gap override. Default \code{NULL}.}

\item{gap_y}{Numeric. Vertical gap override. Default \code{NULL}.}

\item{standardize}{Logical. Equal-width columns. Default \code{FALSE}.}

\item{residuals}{Logical. Compute Pearson residuals. Default \code{FALSE}.}

\item{size}{Text size. Default \code{3.5}.}

\item{colour}{Text colour. Default \code{"black"}.}

\item{label.padding}{Amount of padding around label. Default
\code{ggplot2::unit(0.15, "lines")}.}

\item{na.rm}{Logical. Remove missing values. Default \code{FALSE}.}

\item{show.legend}{Logical. Show legend. Default \code{FALSE}.}

\item{inherit.aes}{Logical. Inherit aesthetics. Default \code{FALSE}.}
}
\value{
A ggplot2 layer.
}
\description{
Add labels with background to a marimekko plot
}
\examples{
library(ggplot2)

titanic <- as.data.frame(Titanic)
ggplot(titanic) +
  geom_marimekko(
    aes(fill = Survived, weight = Freq),
    formula = ~ Class | Survived
  ) +
  geom_marimekko_label(aes(
    x = Class, fill = Survived, weight = Freq,
    label = after_stat(weight)
  )) +
  scale_x_marimekko()

}
