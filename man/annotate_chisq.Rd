% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-chisq.R
\name{annotate_chisq}
\alias{annotate_chisq}
\title{Annotate a marimekko plot with a chi-squared test}
\usage{
annotate_chisq(
  data,
  x,
  fill,
  weight = NULL,
  pos_x = 0.95,
  pos_y = 0.05,
  size = 3,
  hjust = 1,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Name of the categorical x variable (unquoted or string).}

\item{fill}{Name of the categorical fill variable (unquoted or string).}

\item{weight}{Name of the weight variable (unquoted or string), or
\code{NULL} for unweighted counts. Default \code{NULL}.}

\item{pos_x}{Numeric x position for annotation. Default \code{0.95}.}

\item{pos_y}{Numeric y position for annotation. Default \code{0.05}.}

\item{size}{Text size. Default \code{3}.}

\item{hjust}{Horizontal justification. Default \code{1}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:annotate]{ggplot2::annotate()}}.}
}
\value{
A ggplot2 annotation layer.
}
\description{
Annotate a marimekko plot with a chi-squared test
}
\examples{
library(ggplot2)

titanic <- as.data.frame(Titanic)
ggplot(titanic) +
  geom_marimekko(
    aes(fill = Survived, weight = Freq),
    formula = ~ Class | Survived
  ) +
  scale_x_marimekko() +
  annotate_chisq(titanic, Class, Survived, weight = Freq)

}
