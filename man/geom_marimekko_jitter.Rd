% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-marimekko-jitter.R
\name{geom_marimekko_jitter}
\alias{geom_marimekko_jitter}
\title{Jitter individual observations within marimekko tiles}
\usage{
geom_marimekko_jitter(
  mapping = NULL,
  data = NULL,
  position = "identity",
  ...,
  gap = 0.01,
  gap_x = NULL,
  gap_y = NULL,
  standardize = FALSE,
  size = 1,
  alpha = 0.5,
  shape = 16,
  colour = "black",
  seed = NA,
  na.rm = FALSE,
  show.legend = FALSE,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. Requires \code{x} and \code{fill}.}

\item{data}{A data frame.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Additional arguments passed to the layer.}

\item{gap}{Numeric. Gap between tiles. Default \code{0.01}.}

\item{gap_x}{Numeric. Horizontal gap override. Default \code{NULL}.}

\item{gap_y}{Numeric. Vertical gap override. Default \code{NULL}.}

\item{standardize}{Logical. Equal-width columns. Default \code{FALSE}.}

\item{size}{Point size. Default \code{1}.}

\item{alpha}{Point transparency. Default \code{0.5}.}

\item{shape}{Point shape. Default \code{16} (filled circle).}

\item{colour}{Point colour. Default \code{"black"}.}

\item{seed}{Random seed for reproducible jitter. Default \code{NA}
(different each time).}

\item{na.rm}{Logical. Remove missing values. Default \code{FALSE}.}

\item{show.legend}{Logical. Show legend. Default \code{FALSE}.}

\item{inherit.aes}{Logical. Inherit aesthetics. Default \code{FALSE}.}
}
\value{
A ggplot2 layer.
}
\description{
Jitter individual observations within marimekko tiles
}
\examples{
library(ggplot2)

# Small dataset: UCBAdmissions Dept A
ucb <- as.data.frame(UCBAdmissions)
ucb_a <- ucb[ucb$Dept == "A", ]
ggplot(ucb_a) +
  geom_marimekko(
    aes(fill = Admit, weight = Freq),
    formula = ~ Gender | Admit
  ) +
  geom_marimekko_jitter(aes(x = Gender, fill = Admit, weight = Freq)) +
  scale_x_marimekko()

}
